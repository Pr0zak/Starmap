# Single source of truth for the app version.
# Bumped automatically by .github/workflows/bump-version.yml.
# VERSION_NAME is also derived from the git tag (vX.Y.Z) at release time.
VERSION_NAME=1.0.0
VERSION_CODE=1
