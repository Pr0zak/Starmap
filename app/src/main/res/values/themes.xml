<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Host theme for the Activity window. The UI itself is drawn with Compose
         Material 3; this only needs to be a no-action-bar theme with the right
         window background so there is no flash before Compose draws. -->
    <style name="Theme.Starmap" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/deep_space</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>
